import styled from 'styled-components/macro';

const StName = styled.b`
  font-style: normal;
  font-weight: normal;
  font-size: 18px;
  line-height: 22px;
  color: #000000;
  margin-left: 8px;

  em {
    background: #ffffca;
  }
`;

export default StName;
