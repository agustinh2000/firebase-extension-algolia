import styled from 'styled-components/macro';

const StPostCard = styled.div`
  padding: 18px 17px;
  margin-top: 20px;
  position: relative;
  width: 40%;
  height: auto;
  background: #ffffff;
  box-shadow: 0px 4px 9px rgba(0, 0, 0, 0.1);
  border-radius: 8px;

  a {
    text-decoration: none;
  }
`;

export default StPostCard;
