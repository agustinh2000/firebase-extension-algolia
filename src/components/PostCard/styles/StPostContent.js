import styled from 'styled-components/macro';

const StPostContent = styled.span`
  font-style: normal;
  font-weight: normal;
  font-size: 18px;
  line-height: 22px;

  em {
    background: #ffffca;
  }
`;

export default StPostContent;
