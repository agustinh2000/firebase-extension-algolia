import styled from 'styled-components';

const InputFile = styled.input`
  position: relative;
  text-align: right;
  opacity: 0;
  z-index: 2;
  cursor: pointer;
  height: 46px;
  max-width: 200px;
`;

export default InputFile;
